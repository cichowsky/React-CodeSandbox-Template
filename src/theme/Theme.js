import { fontSize, fontWeight } from './Variables';

const theme = {
  primary: 'hsl(0, 0%, 100%)',
  secondary: 'hsl(0, 0%, 100%)',
  accent: 'hsl(0, 0%, 100%)',
  textColor: 'hsl(0, 0%, 0%)',
  fontSize,
  fontWeight,
};
export default theme;
